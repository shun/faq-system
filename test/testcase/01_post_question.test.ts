import { createTestClient, ApolloServerTestClient } from 'apollo-server-testing';

describe('post question', (): void => {
  /////////////////////
  // setUp and tearDown
  //
  beforeEach(() => {
  });

  afterEach(() => {
  });

  beforeAll(() => {
  });

  afterAll(() => {
  });

  /////////////////////
  // test
  //
  test('shoud store the new question', (): void => {
  });
  test('get the stored question', (): void => {
  });
